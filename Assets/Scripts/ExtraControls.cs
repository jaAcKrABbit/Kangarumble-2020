using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class ExtraControls : MonoBehaviour
{
    void Start()
    {
        
    }

    void Update()
    {
       /* // press ESCAPE to return to opening menu
        if (Input.GetKey(KeyCode.Escape) || Input.GetKey("joystick 1 button 6"))
        {
            SceneManager.LoadScene("OpeningMenu");
        }*/
    }
}
